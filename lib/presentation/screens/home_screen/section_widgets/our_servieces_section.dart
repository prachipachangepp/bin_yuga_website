import 'package:flutter/material.dart';

class OurServicesSection extends StatelessWidget {
  const OurServicesSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
