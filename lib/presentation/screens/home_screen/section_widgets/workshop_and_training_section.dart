import 'package:flutter/material.dart';

class WorkShopAndTrainingSection extends StatelessWidget {
  const WorkShopAndTrainingSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
